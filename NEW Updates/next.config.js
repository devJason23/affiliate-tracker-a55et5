/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.ibb.co'],
  },
  experimental: {
    esmExternals: 'loose'
  }
}

module.exports = nextConfig
lias,
      '@': require('path').resolve(__dirname)
    };
    return config;
  }
}

module.exports = nextConfig
